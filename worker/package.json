{
  "name": "clidanfi-avisos",
  "version": "1.0.0",
  "private": true,
  "description": "Worker de Cloudflare que manda los recordatorios de CLIDANFI",
  "type": "module",
  "scripts": {
    "vapid": "node scripts/generar-vapid.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=20"
  }
}
